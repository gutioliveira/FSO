#include "geometric_operations.h"
#include <stdio.h>

struct dot * data_entry();

extern void data_output(const struct dot dots[]);

extern void print_perimeter(const struct triangule triangule);

extern void print_area(const struct triangule triangule);

extern void print_sides(const double * sidess);